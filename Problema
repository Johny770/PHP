<?php 
function oddSum($n) 
{ 
    $sum = 0; $curr = 1; 
    for ($i = 0; $i < $n; $i++)  
    { 
        $sum += $curr; 
        $curr += 2; 
    } 
    return $sum; 
}  
$n = 5; 
echo " Suma primelor ", $n ,
      " Numere impare este: ", oddSum($n);
?>
